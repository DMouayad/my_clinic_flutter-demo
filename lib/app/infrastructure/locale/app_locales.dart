import 'dart:ui';

class AppLocales {
  static const fallbackLocale = Locale('en');
  static const supportedLocales = [
    Locale('en', 'US'),
    Locale('en'),
    Locale('ar'),
    Locale('ar_AE'),
  ];
}
