import 'package:fluent_ui/fluent_ui.dart';

class AdminPanelWindowsScreen extends StatelessWidget {
  const AdminPanelWindowsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
